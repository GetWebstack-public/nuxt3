apiVersion: v1
kind: Service
metadata:
  name: nuxt3
  namespace: nuxt3
  labels:
    app: nuxt3
spec:
  type: ClusterIP
  selector:
    app: nuxt3
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
